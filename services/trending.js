const router = require('express').Router()

const Feed = require('../routes/api/feeds/feeds.model');

const trendingFeed = async(req,res,next)  =>{
    try {
        
    } catch (error) {
        
    }
}


router.post('/', trendingFeed);

module.exports = router